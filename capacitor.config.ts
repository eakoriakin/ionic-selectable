import { CapacitorConfig } from '@capacitor/cli';

const config: CapacitorConfig = {
  appId: 'io.ionic.selectable',
  appName: "Ionic Selectable",
  webDir: 'www',
  server: {
    androidScheme: 'https'
  }
};

export default config;
