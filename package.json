{
  "name": "root",
  "private": true,
  "devDependencies": {
    "lerna": "~4.0.0"
  },
  "scripts": {
    "bootstrap": "lerna bootstrap",
    "version": "lerna version --no-private",
    "publish": "lerna publish --force-publish",
    "publish-no-git-reset": "lerna publish from-package --force-publish --no-git-reset"
  }
}
